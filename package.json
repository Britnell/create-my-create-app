{
  "name": "create",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.html",
  "type": "module",
  "alias": {
    "process": {
      "global": "{}"
    }
  },
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=build/app.js --loader:.js=jsx",
    "build:dev": "esbuild src/index.js --sourcemap --outfile=build/app.js --loader:.js=jsx",
    "build:prod": "esbuild src/index.js --bundle --minify --outfile=build/app.js --loader:.js=jsx",
    "build:script": "node ./build.cjs",
    "dev": "node ./serve.cjs",
    "format": "prettier --write \"src/**/*.{js,jsx}\"",
    "lint": "eslint \"src/**/*.{js,jsx}\" --quiet",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.15.10",
    "eslint": "^8.24.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-jsx-a11y": "^6.6.1",
    "eslint-plugin-react": "^7.31.8",
    "parcel": "^2.7.0",
    "prettier": "^2.7.1"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "browserslist": [
    "> 0.5%, last 2 versions, not dead"
  ]
}
